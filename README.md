CMDV-Testing gh-pages
=====================

The `index.html` file can be updated by running

    $ jupyter notebook

and modifying `index.ipynb`.  When that is completed, run

    $ jupyter nbconvert index.ipynb

and the new `html` file will be generated.
